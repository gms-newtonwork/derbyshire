/**
 * Consistent colour identity for "where did this number come from" —
 * Historical Inputs vs Scenario Inputs — used throughout tables, charts and
 * page accents. Validated as a CVD-safe categorical pair (see dataviz skill
 * palette validator); always paired with a text label, never colour alone.
 */
export const HISTORICAL_COLOR = '#0891b2';
export const SCENARIO_COLOR = '#c026d3';
